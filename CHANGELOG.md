# 更新日志

## 0.17.21（本地定制版）
- **新增：七猫小说的段评**（此前只有番茄 / QQ阅读）。七猫的协议与前两家完全不同，三处差异都实测钉死后才落码：
  - **定位方式是内容指纹而不是段号**：`/para?source=qm` 回的 `paragraph_id` 是 `md5(段落原文)[:8]`。口径实测（《全球灾变之末日游戏》第 1 章 198 段）：按「去掉首尾空白后的段落原文」算 md5 能命中 196 个且与段落顺序逐一对齐，不处理只命中 1 个。关键在首尾空白——七猫每段以两个全角空格（U+3000）开头，而 Lua 的 `%s` **不匹配 U+3000**，所以归一化把全角空格 / NBSP 的字节显式写进字符类。指纹对不上的段（约 2 段/章，后端文本与当前正文有细微版本差异）自然落空不出气泡，绝不会把评论错位挂到别的段上。
  - **item_id / content_md5 只在目录给的原始章节 url（curl）里**，不在章节 url 载荷的固定字段里（载荷字段集不能随便加：一改已落盘目录就会解包错位），所以从 curl 解析；缺 content_md5 时计数直接早失败并说明原因，不让读者白等一次注定失败的网络往返。
  - **评论一次给全、没有分页**：`/paras/proxy_qmidea?action=paragraph_first` 把一段的评论分成 hot_zone（实测 5 条）与 time_zone（实测 30 条）两区，合并按 comment_id 去重（网页端同款做法）；续拉接口 `paragraph_more` 实测无论怎么传 `next_id`/`hot` 都回 0 条、`paragraph_first` 顶层 `next_id` 恒为空（书山后端的七猫分页是坏的，网页端的「加载更多」同样出不来），所以「继续加载」在七猫上不出现——与 QQ 同策略。
- 指纹计算用 leko 自带的纯 Lua `Leko/Digest`（不依赖 KOReader 的模块布局），在分页子进程里一章几百段的 md5 开销可忽略；计数结果换算成与番茄 / QQ 完全相同的 `{段号 = 计数}` 格式，分页器与弹窗不需要知道平台差异。
- 离线单测 `tools/lua_para_test.js` 扩到 **157 项 + 10 项结构检查**：新增「七猫」一节 23 项（curl 解析、指纹口径含全角缩进交叉验证、指纹→段号映射、计数 URL 带 content_md5、缺 md5 早失败、评论一次给全 35 条、无下一页、越界/空白段算不出指纹时如实报错）。测试沙箱为真跑 `Digest.lua` 补了 LuaJIT 语义的 `bit` 垫片（fengari 是 32 位有符号整数，`0xffffffff` 字面量是浮点、直接进位运算会抛错；`rshift` 按逻辑右移用除法模拟），md5/sha1/sha256 输出与 Python hashlib 逐字一致。全量回归 **331 项**通过。

## 0.17.20（本地定制版）
- **去掉段评弹窗里的「分段提示」**。0.17.18 引入的想法式弹窗，在「段落号与服务端对不上」时会往列表最前面插一条 `⚠ 这一段的分段与服务端不一致，下面的评论只作参考。`——现在整条移除。读者点开弹窗是为了看评论，不是看我们的分段假设对不对；而实测真正会踩到它的场合（番茄的 `para_src_content` 回 `"1"` 这类短 token、QQ 的 `lineContent` 为空）本来就已被判成「服务端没给原文」，剩下的触发面既窄又不可预期。
- 顺带清掉**只为这条提示存在**的整条判定链：`ParaComments.verifyAlignment` 连同注释一并删除，ReaderView 侧的 `options.alignment` / `ctx.alignment` 传递也随之去掉（续拉回调不必再把上一次的比对结果带下去）。段号越界依旧会被丢弃（0.17.18 的行为不变），只是不再向读者播报。
- 离线单测相应调整：删掉 7 条「对齐校验」断言；另有三处原本借 `verifyAlignment` 表达契约的断言，改为直接断言 `fetchComments` 的返回值（短 token 噪声被过滤成空串、服务端回传的段落原文非空）。段评单测 **134 项 + 10 项结构检查**通过，全量回归 **308 项**通过。

## 0.17.19（本地定制版）
- **段评取数改到子进程，不再冻结界面**（参考番茄插件的做法：横幅提示 + 请求丢子进程）。原来段评是同步阻塞的：服务端每页稳定 4–5 秒（实测 `count=20` 均值 4.3s，`count=40` 直接被拒），首屏还要连拉两页，于是「翻章自动补计数」和「点气泡拉正文」都会让整台机器几秒内不响应任何输入。现在新增 `Leko/AsyncParaReview`，把计数与评论两类请求都交给子进程跑，父进程轮询、到点回调；并发仍走 `Leko/ProcessBudget` 排队（不能自开一条路，Kindle 上多个子进程同时 fork 会把内存峰值推上去、严重时框架重启）——正文走前台道（读者刚点的），自动补计数走后台道（前台书籍任务要跑时让路）。
- **可取消的等待提示**：延迟 0.4 秒才出现（缓存命中只要一两百毫秒，一上来就盖遮罩会白闪），出现后**点一下就能取消**，不用干等；请求在换章 / 关阅读器后回来会被 token 判为过期直接丢弃，不会在别处凭空弹出弹窗。
- **修复：排队超时会把 ProcessBudget 的额度漏掉**（这条是异步化新引入的缺陷，被单测设计时逮到）。排队期间票只在 ProcessBudget 的队列里，超时后不退票的话，这张票将来一定被派发：那时 `spawn` 会真的 fork，而 `_poll` 一看 `finished` 就立刻返回、永远走不到释放，于是这条 lane 被一张不会释放的票堵死——之后自动补计数乃至前台书籍任务都排不上队。现在超时会退票，并置 `cancelled` 让「万一已经派发到」的那次 spawn 立刻把票还回去。
- **修复：续拉可能把同一批评论重复追加**。续拉点改为永远取**缓存尾部**（以服务端游标为准，因为它会跳过被丢掉的纯图片评论），调用方给的 `cursor` 只用来表达「我这不是首次打开」，不参与定位。原实现允许从已加载区间的中间再拉一次，而缓存是读-改-写，重复条目会一直留在里面。
- **镜像记忆要同步回父进程**：子进程里选中的镜像只写在它自己的 source 副本上，不同步回来会有两处代价——下一次 `hostOrder` 又把失效镜像先试一遍（白等一个注定失败的往返，正是「很慢」的一部分），以及「这批计数是不是当前镜像取的」永远判成过期，每翻一章都重拉一次。现在子进程把实际使用的镜像一并回传，父进程用 `rememberSourceHost` 写回 `source.login_info`。
- **气泡规格对齐番茄插件**：番茄侧的 CSS 是 `a.para-comment { font-size: 0.8em; font-weight: bold; margin: 0 1px }`，现在 leko 的气泡也改成**正文字体族 × 0.8 加粗**并留一道缝（原来是 `smallinfofont` 0.72 不加粗）。分页器预留宽度与渲染落点共用 `geometry` 里同一份 face / bold / gap：`bold` 必须两侧一致——`TextWidget` 内部会走 `Font:getAdjustedFace`，正文那一族若存在真实粗体变体，字宽会变，只在一侧加粗会让气泡偏出命中区、点气泡变成翻页。
- 离线单测 `tools/lua_para_test.js` 扩到 **141 项 + 10 项结构检查**。新增第 10 节「异步取数层」：沙箱里 `ProcessBudget` 与 `SubprocessPayload` 都是**真实现**在跑（只有 fork 与时钟是假的），覆盖「计数表带 pid=0 跨 JSON 边界不走形」「评论首屏两页 / 续拉从缓存尾部接上」「取消后回调不再开窗且额度归还」「重复发起顶掉旧请求」「排队超时退票（含反证：裸映射过 JSON 后读不到 pid0）」；另加 10 项静态结构检查钉住 ReaderView / Paginator 的关键接线与气泡规格。全量回归 **315 项**（另 10 项结构检查）通过。

## 0.17.18（本地定制版）
- **段评弹窗改为微信读书「想法」式富排版**——弹窗顶部是这一段的引文，之后每条评论是「▸ 昵称 · ♥赞」一行加正文，长内容在底部弹窗内上下滚动；点左右半屏翻页、点弹窗外或按返回键关闭。排版管线（分段 / 分页 / 位图缓存）移植自番茄插件的 review_popup，字体与字号直接跟随当前正文排版（`style.body_font` / `body_face.size`），不走 crengine 解析。分段对不上时在最前面插一条提示，评论只作参考（该提示已于 0.17.20 移除）。
- **修复：不管多少条评论，点开只有 20 条**。三处成因：
  - 服务端只稳定接受 `count=20`（`count=30/40` 一律回「获取评论失败」，`count=50` 三成概率挂 20 秒以上），所以「看到更多」不能靠调大单页，改为**首次打开连拉两页**，底部再挂「继续加载（还剩 N 条）」按服务端 `cursor` 续拉。
  - **镜像会对同一段回「空壳」响应**：`200` + 空 `data_list` + 连 `common_list_info` 都没有（实测 v3 时好时坏，同时刻 v4 正常）。原先的校验只看 `data` 是不是表，于是请求停在坏镜像并把空页当成结果。现在要求响应「要么真有条目，要么明确 `has_more=false`」，不满足就当这台没给出数据、继续轮换下一台。
  - 第二页失败不再被当成「到底了」：保留已取到的内容并保持 `has_more=true`，读者再点一次即可重试同一游标。
- **修复：续拉可能让条数反而变少**。评论缓存按镜像分桶（同一段在不同镜像上是两份数据），原生源在某次请求里换镜像后，当前桶里没有「上一次那批」；若照着调用方给的游标直接拉，会交出一份中间缺几十条的列表。现在游标只在「本地这批确实覆盖到那里」时才作数，否则退回从本地尾部接着拉（缓存为空时即重新连拉两页），绝不交出带空洞的列表。
- **修复：协议参数不能带小数点**。`cursor` / `para` / `count` 改用整数格式化：`tonumber(服务端 JSON)` 得到的是浮点数，某些实现下 `tostring` 会给出 `"20.0"`，服务端不接受（段号本身也是从分页器一路算出来的）。
- **段评气泡更小、超过 99 显示 99+**：气泡字号改为正文字号 ×0.72（下限 11），并只按气泡自身字高占位，不再按正文行高撑开；计数超过 99 时显示 `[99+]`，三位数不会把段末行的留白撑宽。QQ 阅读侧服务端不支持翻页，一律不显示「继续加载」（否则同一批评论会被追加一遍）。
- 离线单测 `tools/lua_para_test.js` 扩到 **105 项**：新增「首次打开连拉两页」「续拉只请求一页且不重复前面几页」「热段落（后端报十万条）三页链」「坏镜像空壳响应必须被拒并继续轮换」「部分失败不宣称到底」「旧缓存（无续拉位置）兼容」六组；测试桩件的 `requestJson` 改为与真实现同语义（validate 通过才停下，可指定某台镜像返回什么）。真实响应 fixture 补 `comments_fq_p2.json`（同段第二页）与 `comments_hot_p1/p2/p3.json`（《十日终焉》第1章 para=0，后端报 105118 条）、`comments_hot_degraded.json`（空壳响应按实测形状复刻）。全量回归 279 项通过。

## 0.17.17（本地定制版）
- **新增：段评（段落评论气泡）—— 番茄小说 + QQ阅读**。排版菜单里打开后，本章有评论的段落末尾会出现 `[N]` 气泡，点气泡即读该段评论；另有「本页段评」一次列出本页有评论的段落。
  - 数据走书山聚合后端：计数 `GET /para?source=<fq|qq>&book_id=&chapter_id=`；
    评论正文 番茄 `/idea_comment?api=1&...`、QQ `/qq_comment?ajax=1&bid=&cid=&para=`。
  - 请求全部走原生书山驱动的通道（镜像轮换 / 凭据刷新 / 设备头统一），段评与正文天然落在同一镜像。
  - 排版：分页器只为「本段最后一行」预留气泡宽度（`fitBodyLines` 的 `tail_reserve`），
    末行装得下就完全不重排 ⇒ 不开段评时排版与之前逐像素一致。
  - 命中区按 OverlapGroup 实测落点建屏幕矩形，气泡优先于左右翻页分区判定。
  - 计数/评论都有内存 + 磁盘缓存（计数 1h、评论 6h），失败只在模型上留「本次会话别再试」标记，不阻塞翻页。
  - 实测：番茄的 `para_src_content`、QQ 的 `lineContent` 常为空或短 token ⇒ 段落对齐无法判定时按「无法判定」处理，
    并丢弃越界段落号，避免把评论挂错段或误报「分段不一致」。
  - **平台识别必须解 url 载荷**：书山目录落盘后章节只剩 `{title, url}`，`source`/`bookid`/`itemid`
    打包在 url 的 base64 载荷里（`CHAPTER_FIELDS`/`unpack_fields`）。只看 `chapter.source`
    会让「重启阅读器之后」的所有章节都判成「不支持段评」（装上了却永远不出气泡）；
    现在按需向 `Shushan:parseUrl` 解一次并按 url 记忆，内存章节不受影响。
- 新增离线单测 `tools/lua_para_test.js`（68 项，真实响应 fixture，含「落盘章节」与「镜像分桶」两组回归）；
  `tools/lua_shushan_test.js` 补章节载荷 pack/unpack 往返（真机 toc.lua 原文载荷）；全量回归 242 项通过。
## 0.17.16（本地定制版）
- **目录缓存格式版本化（TOC_FORMAT=2）**——0.17.15 改了章节描述符编码，但书架上已添加的书仍持有旧编码的持久化目录，QQ阅读等非番茄源开书依旧 503。现在 Storage 的目录缓存带 toc_format 版本，旧缓存一次性失效并自动重新拉取（主目录 + 阅读档案目录）。
## 0.17.15（本地定制版）
- **修复：非番茄源目录为空/正文失败——对齐 legado 书山新版协议**——书山 /catalog 对多数站点不再返回章节 url，legado 客户端用 buildChapterUrl 现场拼请求参数。leko 原来强制要求 item.url 非空 ⇒ 爱下/猫眼/速读谷等 20+ 站全部「目录规则返回空内容」；且 /content 请求体多了 name、非番茄分支用 book_id 而非 bookid/chapterid ⇒ 书旗/QQ阅读 503。现在：目录不再依赖章节 url（卷头跳过、按 legado 规则拼 cid/source/url(book)/bookid/chapterid/item_id/qm_url）；正文请求体与 legado 完全一致；响应 content 为 base64 时自动解码。PC 实测：书旗/爱下/猫眼/速读谷/圣武《诡秘之主》第1章全部 200 出正文。
## 0.17.14（本地定制版）
- **搜索排序：作者多数投票加权 + 镜像不去重（Sean 要求逐行展示）**——PC 实测《十日终焉》精确同名 28 行里 22 行是杀虫队队员真书（21+ 站点镜像）、6 行是同名假书（晋江同人或 1 章短故事）。rankedSearchResults 对精确同名行做作者投票：得票最多的作者（原书作者）加权置顶、作者为空小幅保留、不同作者（假书）沉底；同书镜像不再合并、逐行展示。
## 0.17.12（本地定制版）

- **修复：聚合源仍只出 1 条（第 5 个、真正的收尾点）**——LegadoSource 搜索结果解析循环里，global 关键词搜索遇到第 1 个 1000 分精确命中就 break（该早退只为换源设计）。设备文件日志铁证：ranked=10 但 UI 只收到 1 行。现在 global 模式不再提前 break，解析满「每源上限」。

## 0.17.11（本地定制版）

- **正文请求最多试 3 台镜像**（原为全池 6 台 × 30s，最坏 3 分钟，必被前台 45s 看门狗杀掉，表现为「书籍处理连续 45 秒没有新进展」）。快速失败并给出真实原因。

## 0.17.10（本地定制版）

- **错误透传：正文 201/403 不再只显示裸状态码**——书山服务端用非 200 夹带业务原因（实测 201=「VIP会员专享」），requestJson 现在解析响应 body 展示真实原因；VIP/付费/版权类原因直接终止镜像遍历，提示改为「换其他站点版本」。

## 0.17.9（本地定制版）

- **修复：聚合源仍只出 1 条（真正的根因）**——LegadoSource:search 懒解析分支里 global 模式  只保留 1 行并设 ，之前两层修复都在它下游。改为 ：扫描窗口扩到 60 行，去重排序后保留最多 N 行；精确同名命中仍提前结束。

## 0.17.8（本地定制版）

- **修复：聚合源仍只出 1 条（0.17.7 修复不彻底）**——inspect_limit 截断发生在去重之前，书山把同一本书的几十个站点镜像排在最前，前 N 条去重后只剩 1 行。改为对全量结果打分排序去重后再截断。

## 0.17.7（本地定制版）

- **修复：聚合源搜索只出 1 条**——global 模式每个书源只保留 1 条「最佳结果」，书山这类聚合源（单源返回几百条）被压成 1 行。改为 ：按相关性打分排序、同书（归一化标题+作者）去重、每源最多保留「每条书源最多 N 个结果」条。
- 新增 identity 断言（去重/排序/截断），全套 166 checks 全绿。

## 0.17.6（本地定制版）

- **修复：搜索结果仍只显示书源名**——origin 字段被中间层白名单（compactResult / SearchResultCache / 规则抽取）丢掉，三处放行后才真正到达结果行。

## 0.17.5（本地定制版）

- **搜索结果显示底层站点**：书山聚合的每条结果右侧现在显示真实来源（番茄小说 / QQ阅读 / 七猫……），不再只显示「书山聚合（原生）」。
- **只保留小说**：搜索结果过滤掉非 `novel` tab（漫画/短剧等不进结果）。
- 最新章节名不再重复拼站点前缀。

## 0.17.4（本地定制版）

- **修复开书必崩（全平台）**：0.17.3 里 `AsyncBookOperation` 调用了 `Util.tmpname()` 却漏了 `require("Leko/Util")`，全局 `Util` 为 nil，点开任何书都会弹「界面操作失败」（Kindle 与模拟器均复现）。已补上 require。

## 0.17.3（本地定制版）

修「搜索能出结果，但点开书崩溃」。MuMu 实测：0.17.2 之后搜「十日终」能出《十日终焉》，点开即弹「界面操作失败：unable to generate a unique filename」。

- **根因**：`AsyncBookOperation:start` 给后台开书进程生成进度文件时直接调 `os.tmpname()`。Android（bionic）没有可用的 `tmpnam`，该调用直接抛 `unable to generate a unique filename`——PC / Kindle / Kobo 不受影响，只有安卓必炸。
- **修复**：新增 `Util.tmpname()`——先试 `os.tmpname()` 并验证返回路径真的可写（先建后删），不可用就回退到 `<leko>/cache/leko-tmp-<时间>-<计数>.tmp`（各平台都挂在 KOReader 数据目录下，父子进程都可写）。`AsyncBookOperation` 与 `CryptoCompat` 的 3 处 `os.tmpname()` 全部替换（后者是 openssl CLI 兜底路径，防患于未然）。
- **回归**：新增 `tools/lua_tmpname_test.js`（7 项断言，fengari 模拟 Android 两种失败模式：tmpname 抛错 / 返回不可写路径），连同既有三套共 **159 项断言全绿**。

## 0.17.2（本地定制版）

修「全局搜索返回 0 个结果」与三处会崩的隐藏缺陷。起因是 MuMu 安卓模拟器实测：打开「📚书山聚合（原生）」→ 搜「十日终」，列表永远空，且没有任何报错。

### 全局搜索 0 结果（核心）

- **根因**：`BookIdentity:searchMatch` 里，3 个字及以上的关键词**只接受标题完全相等**（`normalized_title == title_query`），部分匹配分支被 `query_length <= 2` 卡住。书山 `/search` 实测正常返回 596 条（《十日终焉》排第一），但「十日终」对《十日终焉》全部被相关性过滤器丢弃——`AsyncSourceSearch` 不抛错、不记失败源，界面上只剩「找到 0 个结果」，毫无线索。
- **修复**：放开长度闸门，所有长度的关键词都走部分匹配；命中且锚定在标题开头的给 700 分、出现在中间的给 620 分（之前只有 ≤2 字才给 700/620）。精确相等仍走上面独立的 1000 分支，排序契约不变。
- **附带防线**：`AsyncSourceSearch:_finishIfIdle` 在结果为 0 但「已有源成功响应、且没有被跳过」时，补一句「**书源有响应，但没有标题与关键词相关的条目**」，以后这种静默空不再被误读成「源坏了」。

### 三处闭包捕获 nil 导致的崩溃

Lua 的局部变量作用域从声明语句**之后**才开始，所以 `local x = constructor{ callback = function() return x end }` 里回调捕获的是**全局 `x`（nil）**，触发 `attempt to index global 'x'`。这三处都在「取消不可中断的阶段」时才崩，平时不触发，属静默型缺陷：

- `SearchView.lua`：打开搜索结果页（`local view = SearchResults:new{... onBookAdded = function() ... view:... end }`）即崩，正是截图里 MuMu 崩溃的那次。
- `MainMenuView.lua`：取消「导入书源」进度时崩。
- `SourceBackupView.lua`：取消「恢复书源备份」进度时崩。

修复统一为「先 `local x`、再 `x = constructor{...}`」两行写法。

- **工具化**：把这类缺陷固化成 `tools/check_local_capture.js`（基于 luaparse 的 AST 扫描，排除字段名误报），扫全插件 98 个文件 0 命中（含 `QuickJS.lua` 的 `goto`——那是 LuaJIT 语法，luaparse 按 5.1 才报错，属预期，扫描器已豁免）。

### 验证

- 新增 `tools/lua_identity_test.js`（fengari 真跑 `BookIdentity:searchMatch` / `bestSearchResult`，配套 `identity_test_scenarios.lua`）：**30 项断言全绿**，覆盖「片段命中正解书名」「精确命中优先」「作者匹配」「空结果边界」。
- 三套离线测试合计 **152 项断言（identity 30 + shushan 71 + native_diag 51）全绿**。
- `version` 升到 0.17.2；`compatibility_version` 维持 9（本版不触及诊断档案，无需重播）。

## 0.17.1（本地定制版）

修「📚书山聚合（原生）」在书源列表与详情里的**错误诊断文案**。起因是真机截图：

```
状态：可使用
内容类型：文字
需要的功能：需要 JavaScript          ← 错
• 包含可选的登录功能
连接情况 / 连接结果：连接失败          ← 错
说明：没有可探测的 HTTP 地址           ← 错
```

三处都错，而且都是「诊断层不认识原生源」造成的，与书源能不能用无关。`compatibility_version` 由 8 升到 9，升级后会自动重算已存书源的兼容档案（内置源原地更新，保留 `enabled` 与运行态），无需手工操作。

- **能力文案**：`SourceStatus:capability` 现在先问一句「这是不是本机原生源」。是的话直接报 `<源名>内置原生驱动（不执行 JavaScript）`，不再走按规则体推断的 JavaScript 档案——书山的规则体里只有一个 `loginUi`，旧逻辑据此把它判成「兼容运行时 ⇒ 需要 JavaScript」，可实际上上游 JavaScript 一行都不会被加载。
- **不再计入 JS 惩罚**：`LegadoSource:analyzeCompatibility` 对原生源跳过 `jsLib` / 登录闸门 / 「使用内置 JavaScript 兼容运行时」这几条评分，等级保持 A，并改为记录一条正面说明。
- **连接探测改问原生驱动**：`SourceHealth:probe` 遇到 `leko://` 源时不再拿 `searchUrl` 去找 HTTP 地址（它当然找不到，于是恒定报「没有可探测的 HTTP 地址 / 连接失败」），而是调用驱动自己的 `Shushan:probe`，真实访问镜像的 `/detection` 并回填真实耗时与检查地址。列表行与详情随之显示「请求成功 120ms」「检查地址：https://v2.vossc.com/detection」。
- **探测必须待得住子进程的死线**：连接检查跑在 fork 出来的子进程里，`AsyncSourceProbe.SOURCE_DEADLINE = 7s` 到点就杀。因此 `Shushan:probe` 不走 `检测节点` 那种六台全扫，而是有明确的墙钟预算（`PROBE_BUDGET = 5s`、最多 `PROBE_HOSTS = 2` 台），超预算就收手并把已探到的原因列出来。
- **探测只读**：`Shushan:probe` 不调用 `_rememberHost`，不写首选节点。探测在子进程里跑，在那里落盘会和父进程的运行态缓存打架；真正改节点仍由 `检测节点` 按钮负责。
- **200 不等于活着**：镜像可能进程活着而上游已死（实测 v1 的 `/detection` 正常应答「书山聚合」而其 `/search` 返回空），也可能根本是另一台服务器回了 200。故把「这个镜像是活的」抽成 `Shushan:detectionOk`，`checkNodes` 与 `probe` 共用同一条判据（要求 HTTP 200 **且** 响应体含「书山」），避免健康检查说「请求成功」而搜索实际不可用。
- **内置说明源也一并修**：`leko://fixture` 这类本机内置资源现在记录为可用（说明「本机内置资源，不经过网络」），不再被判离线。真正没有 HTTP 地址的普通源仍保留原来的「没有可探测的 HTTP 地址」。
- 普通 HTTP 源的探测路径**逐行未改**（HEAD 优先、5xx 回退 GET、4xx 视为服务器可连接）。
- 离线验证：新增 `tools/lua_native_diag_test.js`（fengari 沙箱同时真跑 `BuiltinSources` / `SourceStatus` / `SourceHealth` / `Shushan` 四个模块源码，配套 `native_diag_preamble.lua` / `native_diag_scenarios.lua`），8 组场景 **51 项断言全绿**：原生源识别、能力文案、故障转移、坏响应体、全不通、探测预算、内置资源、以及普通源行为不变的对照组。该套测试在编写过程中就抓出上面「200 不等于活着」这一条真实缺陷。

## 0.17.0（本地定制版）

新增内置书源**「📚书山聚合（原生）」**：把书山聚合从「书源 JSON + JavaScript」改成 **Leko 自己用 Lua 实现的本地协议源**。`catalog_version` 与 `compatibility_version` 未变动，无数据迁移；`builtin_sources_version` 由 2 升到 3，升级后会自动重播内置源（原地更新，保留 `enabled` 与运行态）。

- 新增 `Leko/Shushan.lua`：登录（`/login`）、搜索（`/search`）、目录（`/catalog`）、正文（`/content`）四个接口的纯 Lua 实现，**不执行上游书源里的任何 JavaScript**，不再依赖 QuickJS，因而没有脚本求值超时、没有 6MB realm 内存上限、也不需要 WebView 处理登录页。
- 走 Leko 既有的本地协议短路钩子：`BuiltinSources:request` 接管 `leko://` 地址（原先只接管 `leko://fixture`，现已放宽到整个 `leko://` 前缀），书源规则退化成普通的 JSONPath 选择器 + 正文的 HTML 选择器，规则引擎其余流程完全不变。`LegadoSource:request` / `requestBinary` 现在会把 `source` 透传给钩子，原生驱动才能读到该源的登录信息。
- **多镜像逐请求轮换**：书山有 6 台镜像（`v1`–`v4`.vossc.com 与两个 IP:7001），各接口的可用性并不一致（实测 `v1` 的搜索上游已失效，返回空结果，而 `v2`–`v4` 正常）。因此不做「一次选定、长期固定」，每次请求都按「上次成功的节点 → 其余节点」的顺序尝试，某台返回业务级空数据就自动换下一台；只有 4xx 才提前中止（换个镜像也修不好一个坏请求）。失败的节点会逐台列进报错信息。
- **凭据失效自动重登**：遇到「缺少登录凭证 / 访问被拒绝 / 登录已过期」这类应答时，自动重新登录并把请求重试一次（每请求只重试一次，避免打转）。
- **登录信息落在书源运行态里**：邮箱／密码／设备ID 存在 `source.login_info`，密钥与当前节点存在 `_api_key` / `_host` 两个内部键（不出现在配置界面）。这些都写在 `sources/runtime/<id>.lua`，重新导入或重播内置源只覆盖规则体，**不会冲掉登录状态**。
- 登录界面（`loginUi`）在原生源上改为**原生按钮**：「登录书山」「检测节点」直接调用 `Leko/Shushan.lua`，不经过 QuickJS；同时跳过 QuickJS 按钮的静态能力闸门，原生源不再受「本机不支持该按钮」限制。
- 正文请求带 `X-Device-Id`（用户填写的已登记设备号）与 `X-Device-Type: android`，协议版本 `12`；正文是服务端直出的纯文本，本地再包成 `<article id="content">` 交给正文规则。缺少设备ID、设备号无效、服务端协议版本升级这三种情况都会给出可读的中文提示。
- **书本身份必须稳定**：Leko 用 `book_id = hashId(source_id .. "\n" .. book_url)` 认书，因此写进 `leko://shushan/book?d=…` 的字段只能是**不会随时间漂移**的（书源 / 阅读地址 / 书名 / 分类 / 作者 / 封面 / 简介 / 标签）。`wordCount` 与 `latestChapterTitle` 被刻意排除：它们随连载更新而变，一旦包进地址，书每次更新都会算成一本新书（书架多出一本、阅读进度接不上）。这两项本来也不需要——`ruleSearch` 直接从搜索响应里取。
- 离线验证：新增 `tools/lua_shushan_test.js`（fengari 沙箱真跑 `Leko/Shushan.lua`，配套 `shushan_test_preamble.lua` / `shushan_test_scenarios.lua` 与真实录制的响应夹具），13 个场景 **71 项断言全绿**，覆盖节点故障转移、密钥失效重登、卷头跳过、设备头与协议版本、正文 HTML 化、缺设备ID 提示、书本身份稳定等。该套测试在开发中就抓出三个真实缺陷：`Storage:saveSourceRuntime` 被以点调用（`self` 错位导致密钥静默落盘失败）、目录返回的数字 `cid` 被格式化成 `"1.0"` 回传给服务端，以及书本身份会被连载更新冲掉。

## 0.16.2（本地定制版）

新增番茄章节的**段评**（段落评论）。数据版本号（catalog / compatibility / builtin_sources）均未变动，无数据迁移。

- 排版菜单新增「段评：开/关」与「本页段评」两个入口，**仅当当前章节是番茄章节**（章节地址里带 `item_id`）时显示，其他书源看不到这一行。
- 开启后，本章「有读者评论」的段落会在段末行的行尾显示 `[N]` 气泡（计数超过 999 显示 `[999+]`）；点击气泡弹出该段的评论列表（评论人 / 正文 / 点赞数 / 作者标识），「本页段评」则汇总当前页所有带评论的段落。
- 新增数据层 `Leko/ParaComments.lua`：`/para` 取每段评论计数（内存 + 磁盘缓存，TTL 1 小时），`/idea_comment` 取评论正文（TTL 6 小时）。分页过程只做纯内存查询，**绝不在排版路径上发网络请求**；请求在页面绘制完成后异步发起，失败只记日志、不打断阅读。
- 段落号对齐：番茄的 `paragraph_id` 与 `Util.splitParagraphs` 产出的非空段落一一对应（0 基）。取评论正文时会用后端回传的 `para_src_content` 做一次对齐校验，对不上就不显示气泡，避免把评论挂到别的段落上。
- 分页器为气泡预留行尾宽度（`fitBodyLines` 新增 `tail_reserve`），且**只在「本段最后一行正好落在本页」时预留**，中间窗口不预留，因此未开启段评时或无需让位时，分页结果与改动前完全一致。
- 新增设置键 `para_review_enabled`（默认关闭）。

## 0.16.1（本地定制版）

- 「跨章净屏」改为调用 KOReader 的全局刷新：跨章翻页不再播放本地黑白条带波，而是整屏 "full" 刷新一次（`Screen.refreshFull`，与 KOReader 对角线滑动的整屏全刷走同一条 UIManager 刷新队列）。
- 该开关与「动画效果」解耦：关闭「动画效果」后同章翻页不再有动画，跨章净屏仍然生效；关闭「跨章净屏」则跨章翻页回到普通局部刷新。
- 移除 `Leko/ChapterWaveRefresh.lua`（条带波的实现与全部调用点），跨章净屏不再创建整屏临时 framebuffer。
- 设置键沿用 `chapter_clean_wave_enabled`，老配置无需迁移。

## 0.16.0（本地定制版）

基于上游 0.16.0（2026-09-09 发布）逐条移植本地定制，功能与 0.15.50 定制版保持一致：

- 翻页动画为“擦除渐显”（wipe）：旧页作底、新页逐条带显现，帧延迟 15ms，设备有硬件原生 swipe 也统一走软件动画。
- 排版设置：字号弹框手填（14–72）、字体粗细（正常 / 粗体两档）、首行缩进（关 / 2 字符 / 4 字符）、上边距 / 下边距独立调节（页眉固定在顶部不随上边距移动）。
- 页眉：左侧章名、右侧时间 + 电量（`[nn%]` 方括号），距屏幕顶 5px。
- 夜间模式：黑底白字（正文、标题、页眉、页脚同步适配）。

同时保留上游 0.16.0 全部更新：

- 中文整字行宽优化：行尾剩余空间平分给左右边距；页边距档位改为保证整列递减（ReaderMargins），修复左右留白不均与档位重复。
- 修复长段落、空白尾段与跨章边界产生的空白页。
- 页眉 / 页脚字号收紧；页眉显示时间。
- 翻页触控区重划：中央小块呼出菜单、左窄条上翻、其余下翻；支持的设备可在排版菜单直接调“屏幕亮度”。
- 接入 KOReader 原生阅读统计。
- 书源与引擎：聚合书源登录（loginUi）支持、目录增量复用（检查更新只重解析新增/变化章节）、搜索与检查更新性能优化、加密体系升级（PureCrypto）。
- 阅读设置菜单标题改为“阅读设置”。

## 0.15.50（本地定制基线，基于 0.15.49）

- 翻页动画改为“擦除渐显”（wipe）效果：旧页作底、新页逐条带显现，动画帧延迟默认 15ms，参考 Swipe_Animation.koplugin 实现；支持硬件原生 swipe 的设备也统一使用该软件动画。
- 排版新增“上边距”“下边距”两个独立设置：上边距调整页眉（或屏幕顶部）到正文的留白，下边距调整正文底部到屏幕下边缘的留白，页眉固定在顶部不随上边距移动。
- 首行缩进支持 2 字符 / 4 字符两档，替代原来的开/关开关，旧设置自动兼容为 2 字符。
- 页眉改为左侧显示章名、右侧显示当前时间和电池电量（电量以 [nn%] 方括号显示），且固定在屏幕顶部不随上边距移动。
- 字号由固定档位改为弹框手填数字（范围 14–72），不再限定几个档位。
- 排版新增“字体粗细”（正常 / 粗体，两档切换）和“夜间模式”（黑底白字，开关）两项设置。

## 0.15.49

- 精确保存并恢复章节内阅读位置，减少退出、重排版和换源后的跳读。
- 修复缓存进度条在排版、跳转等弹窗关闭后消失的问题。
- 补齐 Kindle 实体翻页键映射。
- 优化换源后的返回与续读流程，直接回到当前章节。
- 优化手动优先书源的搜索调度与超时重试顺序。
- 修复已有阅读器中从书籍详情点击“继续阅读”可能导致 KOReader 闪退的问题。
